#include <stdio.h>
#include <string.h>

int main(){
    //write your code here
    int n, k;
    scanf("%d %d", &n, &k);

    int a[n];
    for(int i = 0; i < n; i++){
        scanf("%d", &a[i]);
    }

    for(int i = 0; i < n - 1; i++){
        for(int j = i + 1; j < n; j++){
            
        }
    }
}